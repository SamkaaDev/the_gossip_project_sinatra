<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <h1>Mon super site de gossip !</h1>
    <!-- Displays gossips -->
    <% if gossips.size != 0
          gossips.each_with_index do |gossip, i| %>
          <p>
            <%= gossip.author %><br/>
            <a href="/gossips/<%= i.to_s %>"><%= gossip.content %></a>
          </p>
      <% end
       else %>
       <p><i>Il n'y a pas encore de potin...</i></p>
    <% end %>
    <!-- New gossip link -->
    <p><a href="/gossips/new/">Nouveau potin</a></p>
  </body>
</html>
